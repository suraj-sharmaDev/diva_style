import React from 'react';
import styled from 'styled-components';

const View = styled.View``;
const Text = styled.Text``;

const NotificationPop = ({ appIconSource, appTitle, timeText, title, body }) =>{
	let content = (
		<View>
			<Text>Hello</Text>
		</View>
	);
	return content;
}

export default NotificationPop;